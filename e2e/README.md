# e2e

Destino canónico de Playwright contra stack real (Fase 0+).

Hoy el scaffolding vive en `tests/e2e/` (mapa SC, fixtures). Se migrará aquí cuando el vertical API esté estable.

```bash
cd ../tests/e2e
npm install
npx playwright test
```
