/**
 * Registro ambulatorio UI + CURP: escenarios activos en `registro-ui.spec.ts`
 * (proyecto chromium, Vite + API real). Backend ya validaba CURP; el skip
 * «pendiente backend» quedó obsoleto.
 */
export {};
